# LUDIA NAIL — Astra Final QA Script

목적: `https://ludianail.vercel.app/`와 GitHub `elegancemin2-lang/ludia-nail` 최신 `main`을 기준으로 **출시 전 최종 통합 QA + P0/P1 수정 + 재검증**을 수행한다.

## 작업 원칙
- 새로운 기능을 무분별하게 추가하지 말고 현재 제품을 실제 네일샵 원장이 사용할 수 있는 수준으로 안정화한다.
- 먼저 조사/재현 → 원인 특정 → 수정 → 동일 시나리오 재테스트 순서로 진행한다.
- 실제 브라우저에서 직접 클릭/입력/전환해 확인한다. 코드만 보고 "정상"이라고 판단하지 않는다.
- 명백한 P0/P1 문제는 실제 코드를 수정하고 GitHub main에 반영한 뒤 재검증한다.
- 디자인 방향은 최신 iPhone/iPad급으로 깔끔하고 세련되게 유지하되, 작은 글씨로 정보 밀도를 억지로 높이지 않는다.
- 고객명 / 예약시간 / 시술명 / 가격 / 상태 같은 핵심 정보는 크게, 선명하게, 겹치지 않게 한다.
- 네이버 SmartPlace처럼 실제 계정이 필요한 항목은 성공했다고 가정하지 말고 `실계정 검증 필요`로 구분한다.
- Supabase/RLS/인증 구조는 기존 멀티샵 격리를 깨지 않는다. 서비스 키를 클라이언트에 노출하지 않는다.
- 데이터 손실 가능성이 있는 DB 변경은 임의로 하지 않는다.
- 데모 데이터와 실제 로그인 샵 데이터가 섞이지 않는지 확인한다.

## 필수 환경
다음 뷰포트를 최소 각각 1회 확인한다.
1. PC: 1440×900 이상
2. Galaxy 계열: 360×800, 412×915
3. iPhone 계열: 375×812, 430×932

Light / Dark 두 테마 모두 확인한다.

## P0 — 반드시 전부 통과
### 공통 셸
- 첫 로드 시 Console uncaught error 없음.
- 오늘 / 예약 / 고객 / 더보기 이동 모두 실제 클릭으로 정상.
- PC 좌측 네비 + 모바일 하단 네비 모두 정상.
- 화이트/블랙 테마 전환 정상, 재접속 후 유지.
- 프로필 사진 클릭 → 파일 선택 → 미리보기/저장 → 재접속 유지.
- Service Worker 때문에 구버전 JS/CSS가 계속 노출되지 않음.

### 오늘
- 오늘 화면 진입 정상.
- `+` 빠른 예약 버튼이 실제로 예약 입력창을 연다.
- 다음 예약 카드 클릭 → 예약 상세 정상.
- 예약 상세 `X` / backdrop / ESC 모두 닫힘.
- 예약 상세 상태 변경 버튼 정상.
- 예약 상세의 고객 보기 정상.

### 예약
- 오늘 버튼 정상.
- 주간 날짜 7개 전환 정상.
- 전체 / 직원별 필터 정상.
- 빈 시간 클릭 → 빠른 예약 정상.
- 우측 상단 + → 빠른 예약 정상.
- 빠른 예약 취소 / backdrop / 저장 정상.
- 신규 예약 저장 후 예약표 즉시 반영.
- 예약카드 클릭 → 상세 정상.
- 예약카드 텍스트가 시간/고객명/시술명/가격과 겹치지 않음.
- 예약카드 우측 1/3 아트 이미지 정상. 해당 디자인 이미지가 없으면 기본 이미지가 깨지지 않고 표시.
- 직원 2명 이상이어도 모바일 가로 스크롤 없이 한 화면에 안정적으로 표시.

### 고객
- 고객 화면 진입 정상.
- 검색 입력 정상.
- 고객 카드 클릭 시 오류 없음.
- 예약 생성 과정에서 신규 고객 생성/기존 고객 연결 정상.
- 실제 샵 로그인 상태에서 다른 salon_id 데이터가 보이지 않음.

### ART / 디자인
- ART STUDIO 진입 정상.
- 새 디자인 / 디자인 편집 시작 버튼 정상.
- 손가락 선택 → 선택 손가락만 편집.
- 색상 / 드로잉 / 파츠 / undo / redo가 오류 없이 작동.
- 저장 후 10손가락 프리뷰에 반영.
- 다각도 최종 확인 열기/닫기 정상.
- 디자인 보관함 진입 정상.
- 디자인 등록 열기/닫기 정상.
- 사진 + 이름 + 가격 + 시간으로 저장 가능.
- 로그인 상태에서는 현재 샵에만 저장되고 Private Storage 이미지를 불러올 수 있음.

## P1 — UX/완성도
- PC/Galaxy/iPhone에서 글씨 잘림, 겹침, 어색한 줄바꿈, 가로 overflow 없음.
- 폰트 계층이 명확함: 고객명 > 시간/핵심값 > 시술명/보조정보.
- 클릭 대상 최소 크기 충분.
- modal/sheet X 버튼이 카드/스크롤 뒤에 가려지지 않음.
- bottom nav가 콘텐츠를 덮지 않음.
- Light/Dark에서 대비 부족한 텍스트 없음.
- 예약표 시간선과 카드 top 위치가 실제 시간과 시각적으로 일치.
- 완료/진행/취소 상태가 색상만으로 구분되지 않고 텍스트도 존재.
- 모든 이미지에 깨진 이미지 아이콘이 노출되지 않음.
- 로딩/빈 상태가 디자인적으로 어색하지 않음.

## 네이버 연동
- Bridge/API/UI 코드 경로를 검토한다.
- 신규예약/변경/취소 → LUDIA 반영 로직을 확인한다.
- 리뷰 수집/답글 대기 UI를 확인한다.
- 실제 SmartPlace DOM과 계정이 없으면 실제 동기화 성공으로 판정하지 않는다.
- 결과에는 반드시 `실계정 검증 필요` 항목을 별도 표기한다.

## 보안 확인
- 클라이언트에 Supabase service_role/secret 없음.
- ludia_* 테이블 RLS 유지.
- 샵별 salon_id 격리 확인.
- Private Storage 접근이 샵/사용자 권한을 우회하지 않음.
- 공개 저장소에 .env, 네이버 쿠키/프로필, 비밀키가 없는지 확인.

## 수정 방식
문제를 발견하면:
1. 재현 조건
2. 원인
3. 수정 파일/코드
4. 실제 브라우저 재테스트 결과
를 남긴다.

P0/P1은 가능한 경우 바로 수정한다.
P2 디자인 취향/확장 기능은 별도 제안만 하고 대규모 재설계는 하지 않는다.

## 최종 결과 형식
마지막 보고는 아래 5개 섹션만 간결하게 작성한다.

### 1. 수정 완료
- 실제 수정한 문제와 커밋

### 2. 통과한 핵심 플로우
- 실제 브라우저에서 재검증한 항목

### 3. 남은 문제
- P0 / P1 / P2 구분

### 4. 실계정 검증 필요
- 특히 Naver SmartPlace

### 5. 출시 판단
- `파일럿 가능` / `파일럿 전 수정 필요` 중 하나
- 판단 근거 3~5개

중요: 근거 없이 "정상", "완료", "출시 가능"이라고 하지 않는다. 실제 클릭/콘솔/네트워크/데이터 결과를 근거로 판단한다.
